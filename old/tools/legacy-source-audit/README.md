# Archived legacy source-audit tools

These helpers came from the pre-`main` JSON-bank migration work and are preserved so the committed migration/source-inventory artifacts can be reconstructed or inspected later.

They are not part of the active Quizbank CLI/tooling contract. Current course-bank work should use the JSON banks, validation/review workflows, and promotion rules on `main` rather than rerunning these branch-era inventory scripts by default.
