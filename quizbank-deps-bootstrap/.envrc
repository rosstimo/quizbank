# If using direnv, this auto-activates uv's venv and loads .env
layout python python3
if command -v uv >/dev/null; then
  export UV_NO_SYNC=1
  if [ ! -d .venv ]; then
    echo "Creating .venv via uv sync…"
    uv sync
  fi
  source .venv/bin/activate
fi
dotenv_if_exists
